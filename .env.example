# Entra ID login — see terraform/entra_login.tf for the App Registration
# these values belong to (tenant_id / client_id).
ENTRA_TENANT_ID=
ENTRA_CLIENT_ID=

# Database
DB_PASSWORD=changeme
# Encryption key for field-level AES-256-GCM encryption (bank account details)
# Generate a new key with: openssl rand -base64 32
ENCRYPTION_KEY=xxxxxxxxx

# Client
API_URL=http://localhost:8080

# CORS — restrict to your frontend origin in production
CORS_ORIGIN=http://localhost:3000

# ABR Lookups
ABR_GUID=xxxxxxxxx
